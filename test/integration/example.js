/*
Write integration tests in this folder as shown below.

Chai expect and sinon are available as globals, in addition to mocha globals.
sinon is a sandbox and will automatically be restored after each test.
*/

describe('Example', () => {
	it('should fail', () => {
		expect(true).to.be.false;
	});
});
